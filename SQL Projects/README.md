# 🗄️ SQL Projects

This directory serves as a portfolio of relational database projects, complex query design, and data manipulation workflows. The scripts housed here demonstrate my ability to extract actionable business intelligence and optimize data retrieval from raw datasets.

---

## 🛠️ Core Concepts Demonstrated

*   **Advanced Querying:** Proficient use of multi-table `JOIN` operations, conditional filtering (`WHERE`, `HAVING`), and precise sorting logic.
*   **Data Aggregation:** Implementing aggregate functions like `SUM()`, `AVG()`, and `COUNT()` combined with complex `GROUP BY` structures to evaluate regional and performance metrics.
*   **Subqueries & Nested Logic:** Building nested queries and subqueries to isolate maximum values, calculate running thresholds, and handle multi-level categorical groups.

---

## 📁 Projects Index

*   **[Food Data Analysis](./Food-Data-Analysis/)** — A complete suite of 11 targeted relational database queries analyzing transactional sales performance, localized customer demand patterns, and seasonal revenue trends.
